my_list = [i for i in range(30)]
print(my_list)


# def rec_insert_sort(x, s_list)

def binary_insert_sorted(x, s_list):
    list_len = len(sorted_list)
    floor = 0
    roof = list_len
    needle_idx = (floor + roof) // 2
    while floor != roof
        if x < s_list[needle_idx]:
            floor = (floor + roof) //2
        else:
            roof = (floor+roof) //2

